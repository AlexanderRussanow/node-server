<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <title>Contact page</title>
   <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap" rel="stylesheet">
   <style>
      body {
         display: flex;
         min-height: 100vh;
         align-items: center;
         justify-content: center;
         flex-direction: column;
         font-family: 'Roboto', sans-serif;
         font-size: 20px;
         background: linear-gradient(to left, #FA8072, #90EE90);
         color: #fff;
      }

      nav {
         position: absolute;
         top: 0;
         left: 0;
         right: 0;
         min-height: 50px;
         display: flex;
         justify-content: flex-start;
         background-color: #FFFFFF;
      }

      nav ul {
         margin: 0;
         padding: 0;
         display: flex;
      }

      nav li {
         list-style: none;
      }

      nav a {
         text-decoration: none;
         color: black;
         min-height: 60px;
         display: block;
         padding: 10px 20px;
         line-height: 60px;
      }

      li {
         list-style: none;
      }

      a {
         text-decoration: none;
         color: #fff;
         padding: 10px;
      }
   </style>
</head>

<body>
   <%- include('./../views/common/navbar.ejs') %>
      <h1>Contact page</h1>
      <p>Ahoj💋</p>
      <% if (links) { %>
         <% links.forEach(({name, url})=> { %>
            <li>
               <a href="<%= url %>">
                  <%= name %>
               </a>
            </li>
            <% }) %>
         <% } %>
</body>

</html>